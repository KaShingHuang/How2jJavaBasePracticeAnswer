package 数字与字符串章节;

public class ControlString {
    public static void main(String[] args) {
       /* sample 9
        String sentence="let there be light";
        String splitStr[]=sentence.split(" ");
        for (int i = 0; i <splitStr.length ; i++) {
            char ch[]=splitStr[i].toCharArray();
            ch[0]= java.lang.Character.toUpperCase(ch[0]);
            String newstr=new String(ch);
            System.out.print(newstr+" ");*/

        /*sample 11
        String sentence="peter piper picked a peck of pickled peppers";
        String Spiltstr[]=sentence.split(" ");
        int sum=0;
        for (String str:Spiltstr) {
            if(str.charAt(0)=='p') sum++;
        }
        System.out.println(sum);*/

        /*sample 13
        char word[]="lengendary".toCharArray();
        for (int i = 0; i < word.length; i++) {
            if(i%2==0) word[i]= java.lang.Character.toUpperCase(word[i]);
            else word[i]= java.lang.Character.toLowerCase(word[i]);
        }
        String result=new String(word);
        System.out.println(result);*/

       /* sample 15
        char word[]="lengendary".toCharArray();
        word[word.length-1]=java.lang.Character.toUpperCase(word[word.length-1]);
        String result=new String(word);
        System.out.println(result);*/

        

        }




        }


